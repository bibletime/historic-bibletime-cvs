
# this Makefile.am was automatically generated by bibletime/docs/gen_am.sh
# please do not edit this file but rather run the script to update

EXTRA_DIST =index.html 
install-data-local:
	mkdir -p $(DESTDIR)$(kde_htmldir)/it/bibletime;
	chmod a+r+X -R $(DESTDIR)$(kde_htmldir)/it;
	for file in *.html; do \
	  $(INSTALL_DATA) $$file $(DESTDIR)$(kde_htmldir)/it/bibletime && \
	  chmod a+r $(DESTDIR)$(kde_htmldir)/it/bibletime/$$file;\
	done;
	cd ..;
	chmod a+r+X -R $(DESTDIR)$(kde_htmldir)/it/bibletime/

uninstall-local:
	for f in *.html; do \
	  echo Removing $(kde_htmldir)/it/bibletime/$$f; \
	  rm -f $(DESTDIR)$(kde_htmldir)/it/bibletime/$$f; \
	done; \
	cd .. \
