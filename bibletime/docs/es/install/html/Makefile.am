
# this Makefile.am was automatically generated by bibletime/docs/gen_am.sh
# please do not edit this file but rather run the script to update

EXTRA_DIST =
install-data-local:
	mkdir -p $(DESTDIR)$(kde_htmldir)/es/bibletime;
	chmod a+r+X -R $(DESTDIR)$(kde_htmldir)/es;
	for file in *.html; do \
	  $(INSTALL_DATA) $$file $(DESTDIR)$(kde_htmldir)/es/bibletime && \
	  chmod a+r $(DESTDIR)$(kde_htmldir)/es/bibletime/$$file;\
	done;
	cd ..;
	chmod a+r+X -R $(DESTDIR)$(kde_htmldir)/es/bibletime/

uninstall-local:
	for f in *.html; do \
	  echo Removing $(kde_htmldir)/es/bibletime/$$f; \
	  rm -f $(DESTDIR)$(kde_htmldir)/es/bibletime/$$f; \
	done; \
	cd .. \
