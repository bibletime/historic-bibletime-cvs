
# this Makefile.am was automatically generated by bibletime/docs/gen_am.sh
# please do not edit this file but rather run the script to update

EXTRA_DIST =index.html introduction.html 
install-data-local:
	mkdir -p $(DESTDIR)$(kde_htmldir)/no/bibletime;
	chmod a+r+X -R $(DESTDIR)$(kde_htmldir)/no;
	for file in *.html; do \
	  $(INSTALL_DATA) $$file $(DESTDIR)$(kde_htmldir)/no/bibletime && \
	  chmod a+r $(DESTDIR)$(kde_htmldir)/no/bibletime/$$file;\
	done;
	cd ..;
	chmod a+r+X -R $(DESTDIR)$(kde_htmldir)/no/bibletime/

uninstall-local:
	for f in *.html; do \
	  echo Removing $(kde_htmldir)/no/bibletime/$$f; \
	  rm -f $(DESTDIR)$(kde_htmldir)/no/bibletime/$$f; \
	done; \
	cd .. \
