INCLUDES = $(all_includes)

libprinting_a_METASOURCES = AUTO

noinst_LIBRARIES = libprinting.a

libprinting_a_SOURCES = cprinter.cpp

all_headers = \
cprinter.h \
cprintrendering.h

EXTRA_DIST = $(libprinting_a_SOURCES) $(all_headers)

